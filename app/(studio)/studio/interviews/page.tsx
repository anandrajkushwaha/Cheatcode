import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Mock Interviews" detail="Nothing behind this yet. It needs its own data model before there is a screen worth showing." />;
}
