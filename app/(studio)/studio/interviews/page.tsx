import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Mock Interviews" detail="Not built yet. This is one of the two new surfaces in this phase — it needs its own data model before there is a screen worth showing." />;
}
