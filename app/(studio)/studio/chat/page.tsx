import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="New Chat" detail="The composer on the home screen is wired to nothing yet. Connecting it to the agent is a change of its own." />;
}
