import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Jobs" detail="Already working at the old address while this shell is being built. The job search, filters and scoring all exist — this screen is where they get ported." />;
}
