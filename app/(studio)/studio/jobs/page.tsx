import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Jobs" detail="Next screen to port. The search, the filters and the city matching all work at the old address today." />;
}
