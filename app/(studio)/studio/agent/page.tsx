import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return (
    <StudioPlaceholder
      title="Agent"
      detail="Press the orb in the bottom-right corner to talk — that already works. This screen is the transcript history, and it is the next one to port."
    />
  );
}
