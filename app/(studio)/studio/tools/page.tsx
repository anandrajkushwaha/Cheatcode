import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Tools" detail="The ATS checker and the salary calculator both work today. They move in here once the shell is signed off." />;
}
