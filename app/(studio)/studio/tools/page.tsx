import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Free Tools" detail="The ATS checker and the salary calculator already work. They move in here once the shell is signed off." />;
}
