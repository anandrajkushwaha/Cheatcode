import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Profile" detail="Your profile is editable at the old address. This screen is a placeholder so the sidebar has somewhere to go." />;
}
