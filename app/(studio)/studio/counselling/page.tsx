import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Career Counselling" detail="Not built yet. The second new surface in this phase, and the one whose shape is still the least decided." />;
}
