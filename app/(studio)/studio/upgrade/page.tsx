import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Pro" detail="Payments are not connected yet. Razorpay with UPI Autopay is the next backend job, and it is the one change that also touches live users." />;
}
