import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Your Resume" detail="Templates and your uploaded resumes live at the old address for now. Porting them here is the next screen after the shell settles." />;
}
