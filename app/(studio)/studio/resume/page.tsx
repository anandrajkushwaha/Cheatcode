import { StudioPlaceholder } from "@/components/studio/Placeholder";

export default function Page() {
  return <StudioPlaceholder title="Resume" detail="This is where upload, ATS score, templates and the builder become one flow instead of three destinations." />;
}
